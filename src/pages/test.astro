---
import { getCollection } from "astro:content";
const posts = await getCollection("blog");
console.log("BUILD-TIME-LOG: posts length =", posts.length);
posts.forEach((p) => console.log("top-id:", p.id));
---

<html>
    <body>
        <h1>Test: {posts.length}</h1>
        <ul>
            {posts.map((p) => <li>{p.id}</li>)}
        </ul>
    </body>
</html>
